# RAG layer

The local preview uses deterministic document retrieval for repeatable lessons. Docker Compose includes Qdrant as the vector-service boundary, and the seed fixtures include both normal content and one clearly synthetic instruction-bearing imported note.