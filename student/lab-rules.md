# Lab rules

1. Keep all traffic on `localhost:8080`.
2. Do not point fetch features at public hosts.
3. Do not add real secrets or personal data.
4. Use only the seeded lab accounts and synthetic documents.
5. Record requests, responses, timestamps, and impact.
6. Reset the lab before a new group begins.
7. Treat the vulnerable behavior as a teaching fixture, not a production pattern.