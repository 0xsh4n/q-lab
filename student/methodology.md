# Methodology

```text
Recon → identify AI functionality → map APIs → understand architecture
→ identify trust boundaries → form hypothesis → test → validate
→ chain → determine impact → report
```

For each test, separate what the frontend prevents from what the backend actually authorizes. For AI flows, identify attacker-controlled text, retrieved content, model instructions, tool selection, tool parameters, and the final backend action.