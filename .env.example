# TechDesk AI — lab environment (synthetic values only; never real secrets)
LAB_MODE=vulnerable
DEBUG_LAB=false

# Intentionally weak, shared JWT signing secret (crackable). Lab-only.
JWT_SECRET=techdesk-dev-secret

# Local model provider
LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_MODEL=phi3:mini

# Synthetic database boundary
POSTGRES_DB=techdesk
POSTGRES_USER=techdesk
POSTGRES_PASSWORD=techdesk-lab-only
